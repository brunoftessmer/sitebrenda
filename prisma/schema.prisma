// Banco local para desenvolvimento: SQLite (arquivo dev.db).
// Para produção (ex: deploy na Vercel), troque o provider para "postgresql"
// e aponte DATABASE_URL para um banco gerenciado (Neon, Supabase, etc.),
// já que o filesystem de funções serverless não é persistente.
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id           String        @id @default(cuid())
  name         String
  phone        String        @unique
  passwordHash String
  isAdmin      Boolean       @default(false)
  createdAt    DateTime      @default(now())
  reservations Reservation[]
}

enum ReservationStatus {
  PENDING
  PAID
  CANCELED
}

model Reservation {
  id          String            @id @default(cuid())
  userId      String
  user        User              @relation(fields: [userId], references: [id])
  status      ReservationStatus @default(PENDING)
  totalCents  Int
  createdAt   DateTime          @default(now())
  confirmedAt DateTime?
  numbers     NumberSlot[]
}

enum NumberStatus {
  AVAILABLE
  RESERVED
  PAID
}

model NumberSlot {
  number        Int          @id
  status        NumberStatus @default(AVAILABLE)
  reservationId String?
  reservation   Reservation? @relation(fields: [reservationId], references: [id])
}
